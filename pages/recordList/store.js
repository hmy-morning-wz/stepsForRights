import Store from 'herculex'
export default new Store({
  connectGlobal: true, // 是否关联global
  state: {
  },
  mutations: {
    // LIST_DATA: (state, config) => {
    //   state.list = config
    //   console.log(state.list,8888)
    // },
  },
  actions: {
  },
})
